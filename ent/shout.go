// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/francismarcus/eg/ent/shout"
	"github.com/francismarcus/eg/ent/user"
)

// Shout is the model entity for the Shout schema.
type Shout struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Message holds the value of the "message" field.
	Message string `json:"message,omitempty"`
	// Likes holds the value of the "likes" field.
	Likes int `json:"likes,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShoutQuery when eager-loading is set.
	Edges       ShoutEdges `json:"edges"`
	user_shouts *int
}

// ShoutEdges holds the relations/edges for other nodes in the graph.
type ShoutEdges struct {
	// Author holds the value of the author edge.
	Author *User
	// LikedBy holds the value of the liked_by edge.
	LikedBy []*User
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// AuthorOrErr returns the Author value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShoutEdges) AuthorOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.Author == nil {
			// The edge author was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Author, nil
	}
	return nil, &NotLoadedError{edge: "author"}
}

// LikedByOrErr returns the LikedBy value or an error if the edge
// was not loaded in eager-loading.
func (e ShoutEdges) LikedByOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.LikedBy, nil
	}
	return nil, &NotLoadedError{edge: "liked_by"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Shout) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullTime{},   // created_at
		&sql.NullTime{},   // updated_at
		&sql.NullString{}, // message
		&sql.NullInt64{},  // likes
	}
}

// fkValues returns the types for scanning foreign-keys values from sql.Rows.
func (*Shout) fkValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // user_shouts
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Shout fields.
func (s *Shout) assignValues(values ...interface{}) error {
	if m, n := len(values), len(shout.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	s.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field created_at", values[0])
	} else if value.Valid {
		s.CreatedAt = value.Time
	}
	if value, ok := values[1].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field updated_at", values[1])
	} else if value.Valid {
		s.UpdatedAt = value.Time
	}
	if value, ok := values[2].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field message", values[2])
	} else if value.Valid {
		s.Message = value.String
	}
	if value, ok := values[3].(*sql.NullInt64); !ok {
		return fmt.Errorf("unexpected type %T for field likes", values[3])
	} else if value.Valid {
		s.Likes = int(value.Int64)
	}
	values = values[4:]
	if len(values) == len(shout.ForeignKeys) {
		if value, ok := values[0].(*sql.NullInt64); !ok {
			return fmt.Errorf("unexpected type %T for edge-field user_shouts", value)
		} else if value.Valid {
			s.user_shouts = new(int)
			*s.user_shouts = int(value.Int64)
		}
	}
	return nil
}

// QueryAuthor queries the author edge of the Shout.
func (s *Shout) QueryAuthor() *UserQuery {
	return (&ShoutClient{config: s.config}).QueryAuthor(s)
}

// QueryLikedBy queries the liked_by edge of the Shout.
func (s *Shout) QueryLikedBy() *UserQuery {
	return (&ShoutClient{config: s.config}).QueryLikedBy(s)
}

// Update returns a builder for updating this Shout.
// Note that, you need to call Shout.Unwrap() before calling this method, if this Shout
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Shout) Update() *ShoutUpdateOne {
	return (&ShoutClient{config: s.config}).UpdateOne(s)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (s *Shout) Unwrap() *Shout {
	tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Shout is not a transactional entity")
	}
	s.config.driver = tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Shout) String() string {
	var builder strings.Builder
	builder.WriteString("Shout(")
	builder.WriteString(fmt.Sprintf("id=%v", s.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(s.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(s.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", message=")
	builder.WriteString(s.Message)
	builder.WriteString(", likes=")
	builder.WriteString(fmt.Sprintf("%v", s.Likes))
	builder.WriteByte(')')
	return builder.String()
}

// Shouts is a parsable slice of Shout.
type Shouts []*Shout

func (s Shouts) config(cfg config) {
	for _i := range s {
		s[_i].config = cfg
	}
}
